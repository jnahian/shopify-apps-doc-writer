{
  "name": "shopify-apps-doc-writer",
  "version": "0.2.0",
  "private": true,
  "description": "Claude Code plugin: merchant-facing feature docs for embedded Shopify apps with real, reproducible screenshots.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup-auth": "node scripts/setup-auth.js",
    "capture": "node scripts/capture.js",
    "test": "node --test",
    "typecheck": "tsc"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
